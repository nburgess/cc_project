<%= simple_form_for(@team_invitation) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :email %>
    <%= f.association :team%>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
